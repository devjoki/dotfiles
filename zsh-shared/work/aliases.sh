alias prod_quotes="$HOME/dynamo_queries/query.sh production quoting_quotes"
alias stg_quotes="$HOME/dynamo_queries/query.sh staging quoting_quotes"

# API key is now set in WezTerm config to avoid repeated password prompts
# export ANTHROPIC_API_KEY=$(security find-generic-password -s "anthropic-api-key" -w 2>/dev/null)
